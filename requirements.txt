# ─── Core framework ────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0    # ASGI server with WebSocket support
mangum>=0.17.0               # Adapter for Netlify/AWS Lambda

# ─── Validation (Pydantic v2 is bundled with FastAPI >= 0.100) ────────────
pydantic>=2.6.0

# ─── Optional: testing ───────────────────────────────────────────────────
httpx>=0.27.0                # async HTTP client used by TestClient
pytest>=8.0.0
pytest-asyncio>=0.23.0
